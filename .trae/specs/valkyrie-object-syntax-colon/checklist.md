- [ ] AST 定义已修改：`Expr::Object` 使用 `fields: Vec<(Identifier, Option<Expr>)>` 结构
- [ ] 模式匹配 AST 已修改：`Pattern::Class` 支持 `:` 语法
- [ ] 解析器支持 `:` 分隔符的对象构造语法
- [ ] 解析器支持 `:` 分隔符的模式匹配语法
- [ ] 解析器支持字段简写 `{ x, y }` 语法
- [ ] 使用 `=` 语法时产生弃用警告
- [ ] 构建器逻辑已更新以支持新 AST 结构
- [ ] zh-hans 文档中所有对象构造语法已更新为使用冒号
- [ ] zh-hans 文档中所有模式匹配语法已更新为使用冒号
- [ ] 设计原理说明已添加到文档
- [ ] 新语法的测试用例已创建
- [ ] 现有测试用例已更新使用新语法
- [ ] 所有测试通过
- [ ] 编译无错误
