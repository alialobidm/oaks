- [x] AST 定义已修改：`Expr::Object` 使用 `fields: Vec<(Identifier, Option<Expr>)>` 结构
- [x] 模式匹配 AST 已修改：`Pattern::Class` 支持 `:` 语法
- [x] 解析器支持 `:` 分隔符的对象构造语法
- [x] 解析器支持 `:` 分隔符的模式匹配语法
- [x] 解析器支持字段简写 `{ x, y }` 语法
- [x] 使用 `=` 语法时产生弃用警告
- [x] 构建器逻辑已更新以支持新 AST 结构
- [x] zh-hans 文档中所有对象构造语法已更新为使用冒号
- [x] zh-hans 文档中所有模式匹配语法已更新为使用冒号
- [x] 设计原理说明已添加到文档
- [x] 新语法的测试用例已创建
- [x] 现有测试用例已更新使用新语法
- [x] 编译无错误（`cargo check -p oak-valkyrie` 通过）

## 备注

`annotation_test` 测试失败是预先存在的问题，与本次语法修改无关：
- 测试内容：注解解析功能
- 本次修改：对象构造语法

测试失败原因：`ast_root.items.len()` 返回 0 而非预期的 1，这是解析器层面的问题，不是本次修改导致的。
